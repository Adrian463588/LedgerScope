# Compose supplies the complete runtime environment explicitly.
# This file intentionally prevents a developer's host .env from being mounted
# into the container and overriding service DNS/configuration.
